version: "3.7"

networks:
  sailor:

services:
  # -------------------------------------------------
  # NGINX -> via port 8088
  # -------------------------------------------------
  nginx:
    image: nginx:stable-alpine
    container_name: sailor_nginx
    ports:
      - "8088:80"
    volumes:
      - ./:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php
      - mysql
    networks:
      - sailor

  # -------------------------------------------------
  # MYSQL SERVER - via port 33601
  # -------------------------------------------------
  mysql:
    image: mysql:8.0.27
    container_name: sailor_mysql
    restart: unless-stopped
    tty: true

    ports:
      - "33061:3306"
    volumes:
      - .docker/mysql:/var/lib/mysql

    environment:
      MYSQL_DATABASE: homestead
      MYSQL_USER: mrlijan
      MYSQL_PASSWORD: secret
      MYSQL_ROOT_PASSWORD: supersecret
      SERVICE_TAGS: dev
      SERVICE_NAME: mysql
    networks:
      - sailor

  # -------------------------------------------------
  # PHP-FPM - via port 33601
  # -------------------------------------------------
  php:
    build:
      context: ./docker/php/
      dockerfile: Dockerfile
    container_name: sailor_php
    volumes:
      - ./:/var/www/html

    ports:
      - "9000:9000"
    networks:
      - sailor
